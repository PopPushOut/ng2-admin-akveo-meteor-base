import {Component, HostListener} from '@angular/core';
import template from './dropdownButtons.html';
@Component({
  selector: 'dropdown-buttons',
  template
})

// TODO: appendToBody does not implemented yet, waiting for it
export class DropdownButtons {

  constructor() {
  }
}
